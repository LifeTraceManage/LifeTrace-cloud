[package]
name = "lifetrace-cloud"
version = "0.1.0"
edition = "2021"
description = "LifeTrace self-hosted cloud backend (Rust + Axum + SQLite)"
license = "MIT"

[dependencies]
aes-gcm = "0.10"
ammonia = "4.1"
argon2 = { version = "0.5", features = ["std"] }
async-trait = "0.1"
axum = { version = "0.8", features = ["json", "macros", "multipart", "ws"] }
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
hex = "0.4"
hmac = "0.12"
imap = { version = "3.0.0-alpha.15", default-features = false, features = ["rustls-tls"] }
ipnet = "2"
lettre = { version = "=0.11.22", default-features = false, features = ["builder", "smtp-transport", "tokio1-rustls-tls"] }
lifetrace-contracts = { path = "crates/lifetrace-contracts" }
mail-parser = { version = "0.11.5", features = ["full_encoding", "serde"] }
rand = "0.8"
rpassword = "7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "sqlite", "chrono", "uuid", "json", "migrate", "macros"] }
subtle = "2"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors", "request-id", "set-header", "fs"] }
uuid = { version = "1", features = ["v4", "v5", "serde"] }
url = "2"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
