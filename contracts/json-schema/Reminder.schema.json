{
  "$comment": "GENERATED FILE - DO NOT EDIT MANUALLY. Source: crates/lifetrace-contracts.",
  "$defs": {
    "DeviceId": {
      "description": "Stable device identifier (string, usually UUID v4).",
      "type": "string"
    },
    "EntityId": {
      "description": "Entity identifier. New IDs are UUID v4; legacy non-UUID IDs are preserved verbatim.",
      "type": "string"
    },
    "EntityMeta": {
      "description": "Metadata shared by every syncable entity payload.\n\n`local_version` is the local revision counter (EPIC-01 `version` column).\nIt is NOT a server authority; `server_version` is assigned by the server\nand `base_server_version` (on changes) is what the client knew when it\nmade the change. Offline edits must never fabricate `server_version`.",
      "properties": {
        "createdAt": {
          "format": "date-time",
          "type": "string"
        },
        "deletedAt": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "$ref": "#/$defs/EntityId"
        },
        "localVersion": {
          "description": "Local revision number (never the server-authoritative version).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "modifiedByDevice": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeviceId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Device that last modified the entity locally (audit only)."
        },
        "serverVersion": {
          "anyOf": [
            {
              "$ref": "#/$defs/ServerVersion"
            },
            {
              "type": "null"
            }
          ],
          "description": "Server-authoritative version; `None` until the server first accepts it."
        },
        "updatedAt": {
          "format": "date-time",
          "type": "string"
        },
        "userId": {
          "$ref": "#/$defs/UserId"
        }
      },
      "required": [
        "id",
        "userId",
        "createdAt",
        "updatedAt",
        "localVersion"
      ],
      "type": "object"
    },
    "ReminderStatus": {
      "description": "Reminder delivery lifecycle shared by Cloud and clients.",
      "enum": [
        "scheduled",
        "fired",
        "dismissed",
        "cancelled"
      ],
      "type": "string"
    },
    "ServerVersion": {
      "description": "Server-authoritative entity version. Wire format: decimal string\n(for example `\"42\"`) to avoid JavaScript safe-integer issues.",
      "type": "string"
    },
    "UserId": {
      "description": "Stable user identifier (string, usually UUID v4).",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`execution.reminder`.\n\nThis intentionally preserves the existing Execute wire shape\n(`subjectType`, `subjectId`, `fireKey`) while making it a strict typed\ncontract. The Cloud execution worker advances due `scheduled` reminders to\n`fired`. Clients use the subject fields to route notification taps.",
  "properties": {
    "body": {
      "type": [
        "string",
        "null"
      ]
    },
    "fireKey": {
      "type": "string"
    },
    "lastFiredAt": {
      "format": "date-time",
      "type": [
        "string",
        "null"
      ]
    },
    "meta": {
      "$ref": "#/$defs/EntityMeta"
    },
    "snoozedUntil": {
      "format": "date-time",
      "type": [
        "string",
        "null"
      ]
    },
    "status": {
      "$ref": "#/$defs/ReminderStatus"
    },
    "subjectId": {
      "$ref": "#/$defs/EntityId"
    },
    "subjectType": {
      "type": "string"
    },
    "title": {
      "type": [
        "string",
        "null"
      ]
    },
    "triggerAt": {
      "format": "date-time",
      "type": "string"
    }
  },
  "required": [
    "meta",
    "subjectType",
    "subjectId",
    "triggerAt",
    "status",
    "fireKey"
  ],
  "title": "Reminder",
  "type": "object"
}
